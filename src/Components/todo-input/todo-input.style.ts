import styled from "styled-components";
import { Button } from "antd";

export const TodoAppInputContainer = styled.div`
    input{
        padding: 10px;
    }
`
export const TodoAddButton = styled(Button)`
    margin-top: 10px;
`

